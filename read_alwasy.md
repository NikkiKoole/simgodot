

dont skip type annotations.


Parse Error: Cannot infer the type of ".." variable because the value doesn't have a set type.


try to minimize mocks as much as posssible, always prefer to test real code.
